Tests
=====

* Ensure that rule variables do not get bound themselves (but only instances of them)

isa_thing(thing(X)).

isa_thing(thing("FooBar")) ?
isa_thing(thing("Blah")) ? 

Should both return true.  

* Simple conjunction (AND) test

* Simple disjunction (OR) test
